target/
node_modules/
*.node
*.log
.DS_Store
.env
.env.*
!.env.example
dist/
build/

# napi-rs auto-generates these into the package root on build —
# they ship in published artifacts but aren't committed.
clients/*/index.js
clients/*/index.d.ts

# Tauri build artifacts.
clients/desktop/src-tauri/gen/
clients/desktop/src-tauri/target/
